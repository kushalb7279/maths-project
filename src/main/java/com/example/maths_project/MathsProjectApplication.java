package com.example.maths_project;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication  //what does this annotation do internally?
public class MathsProjectApplication {

	public static void main(String[] args) {
		SpringApplication.run(MathsProjectApplication.class, args);   //what does this line do?
	}

}
